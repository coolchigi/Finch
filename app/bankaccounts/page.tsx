"use client";

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const BankAccounts = () => {

    return (
        <Card>
        <CardHeader>
            <CardTitle>Bank Accounts</CardTitle>
        </CardHeader>
        <CardContent>
            <p>Bank accounts will be displayed here.</p>
        </CardContent>
        </Card>
    );
};

export default BankAccounts;